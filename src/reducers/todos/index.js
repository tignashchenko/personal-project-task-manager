// Instruments
import initialState from 'components/Scheduler/todos';

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
